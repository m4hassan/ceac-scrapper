services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: visa_captcha_solver
    restart: always 
    env_file:
      - ./.env
    command: ["python3.12", "src/scrape.py"]
    ipc: host